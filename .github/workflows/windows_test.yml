name: MSVC on windows test suite

on:
  push:
   branches:
     - master

jobs:
  test:
      runs-on: windows-latest
      steps:
        - name: Checkout repository
          uses: actions/checkout@v3
        - name: test
          run: |
            echo 'testing'
            ls
            cd tests/
            bash build_tests_without_coverage
            ls
            cd cmake-build-default/Debug
            ls
            ./flippy_test
            echo "done testing"
